<UserControl x:Class="Education_Platform.Views.Dashboard.ReferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Education_Platform.Views.Dashboard"
             xmlns:vm="clr-namespace:Education_Platform.ViewModels.Dashboard"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:ReferenceViewModel/>
    </UserControl.DataContext>
    
    <ScrollViewer>

        <StackPanel Margin="20">

            <TextBlock Text="{Binding Title}"
                       FontSize="30"
                       FontWeight="Bold"
                       Margin="0 0 0 25"/>

            <materialDesign:Card Padding="25">

                <TextBlock Text="{Binding Content}"
                           FontSize="18"
                           TextWrapping="Wrap"/>

            </materialDesign:Card>

        </StackPanel>

    </ScrollViewer>
</UserControl>
