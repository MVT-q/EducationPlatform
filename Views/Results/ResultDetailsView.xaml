<UserControl x:Class="Education_Platform.Views.Results.ResultDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Education_Platform.Views.Results"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <ScrollViewer>
        <StackPanel>
            <TextBlock Text="Attempt Details"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="20"/>

            <materialDesign:Card Margin="10"
                                 Padding="20">
                <ItemsControl ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <StackPanel Margin="10">
                                <TextBlock FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="#3F51B5"
                                           Margin="0 0 0 8"
                                           Text="{Binding Number,
                                                  StringFormat=Question {0}}"/>

                                <TextBlock Text="{Binding Text}"
                                           FontWeight="Bold"
                                           FontSize="16"
                                           Margin="0 0 0 15"/>

                                <Border Background="#F8FAFC"
                                        CornerRadius="6"
                                        Padding="10"
                                        Margin="0 10 0 0">


                                    <StackPanel Orientation="Horizontal">
                                        <ItemsControl ItemsSource="{Binding Answers}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    
                                                    <StackPanel Orientation="Horizontal">

                                                        <TextBlock Text="{Binding Text}" Margin="5"/>

                                                        <TextBlock Text="Your answer"
                                                                   Foreground="Blue"
                                                                   FontWeight="SemiBold"
                                                                   Margin="10 5 0 0"
                                                                   Visibility="{Binding IsSelected, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    </StackPanel>

                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <TextBlock Text="{Binding TextAnswer}">

                                            <TextBlock.Style>

                                                <Style TargetType="TextBlock">

                                                    <Setter Property="Visibility"
                                                            Value="Collapsed"/>

                                                    <Style.Triggers>

                                                        <DataTrigger Binding="{Binding Type}"
                                                                     Value="Text">

                                                            <Setter Property="Visibility"
                                                                    Value="Visible"/>

                                                        </DataTrigger>

                                                    </Style.Triggers>

                                                </Style>

                                            </TextBlock.Style>

                                        </TextBlock>
                                    </StackPanel>

                                </Border>

                            </StackPanel>

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
